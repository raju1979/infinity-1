import { EmailDuplicateValidatorDirective } from './email-duplicate-validator.directive';

describe('EmailDuplicateValidatorDirective', () => {
  it('should create an instance', () => {
    const directive = new EmailDuplicateValidatorDirective();
    expect(directive).toBeTruthy();
  });
});
